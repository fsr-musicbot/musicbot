- Using Google's MusicLM to generate music
- Need a `TOKEN` in the shell env. Can get it by logging into the website, inspecting the network request

![token](./docs/token.png)

- Run `python example.py` to generate music
